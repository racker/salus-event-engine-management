stream
  |from()
    .measurement('{{measurement}}')
    .groupBy('{{groupBy}}')
{{#labelsAvailable}}
  |where(lambda:
  {{#labels}}
    if(isPresent("{{labelNamespace}}.{{key}}"), "{{labelNamespace}}.{{key}}" == '{{value}}', FALSE)
    {{^-last}}
      AND
    {{/-last}}
    {{#-last}}
      )
    {{/-last}}
  {{/labels}}
{{/labelsAvailable}}
  |alert()
    .stateChangesOnly()
    .id('{{alertId}}')
    .details('{{details}}')
    .crit(lambda: {{critExpression}})
    .topic('events')