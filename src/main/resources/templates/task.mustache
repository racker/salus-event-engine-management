stream
  |from()
    .measurement('{{measurement}}')
    .groupBy('{{groupBy}}')
  |alert()
    .stateChangesOnly()
    .id('{{alertId}}')
    .details('{{details}}')
    .crit(lambda: {{critExpression}})
    .topic('events')