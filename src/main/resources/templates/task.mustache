stream
  |from()
    .measurement('{{measurement}}')
    .groupBy('{{groupBy}}')
  |where(lambda: {{labelSelectorExpression}})
  |alert()
    .stateChangesOnly()
    .id('{{alertId}}')
    .details('{{details}}')
    .crit(lambda: {{critExpression}})
    .topic('events')